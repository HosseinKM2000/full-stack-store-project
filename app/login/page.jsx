import LoginForm from "<store>/components/auth/loginForm";

const Login = () => {
    return (
        <div className="w-full min-h-screen flex justify-center items-center bg-gray-200">
            <LoginForm/>
        </div>
    );
}

export default Login;